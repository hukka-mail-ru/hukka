#DROP DATABASE TestDB;
CREATE DATABASE IF NOT EXISTS TestDB;

USE TestDB;

CREATE TABLE IF NOT EXISTS tstBlob (dummy INT);
DROP TABLE tstBlob;

CREATE TABLE IF NOT EXISTS tstBlob 
(
	nbr   INT,
        name  VARCHAR(50),
	arr   BLOB
);

DESCRIBE tstBlob;
INSERT INTO tstBlob (nbr, name, arr) VALUES (1, 'test1', 0x1ABC);
INSERT INTO tstBlob (nbr, name, arr) VALUES (2, 'test2', 134567);

SELECT arr FROM tstBlob WHERE nbr = 1;

UPDATE tstBlob SET  arr=0xCBA0 WHERE nbr='1';

SELECT * FROM tstBlob;

CREATE TABLE IF NOT EXISTS tstSelect (dummy INT);
DROP TABLE tstSelect;

CREATE TABLE IF NOT EXISTS tstSelect
(
	nbr   INT,
        name  VARCHAR(50),
        type  VARCHAR(30)	
);

INSERT INTO tstSelect (nbr, name, type) VALUES (1, 'name1', 'type1');
INSERT INTO tstSelect (nbr, type) VALUES (2, 'type2');
INSERT INTO tstSelect (nbr, name, type) VALUES (3, 'name3', 'type3');

SELECT * FROM tstSelect;